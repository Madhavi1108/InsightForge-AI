# InsightForge AI - Phase Status

Single source of truth for build progress. Updated after every phase.

The 69 phases of the master specification (`INSIGHTFORGE AI.pdf`) are tracked here
as a **consolidated 36-phase** roadmap. No scope is dropped - see
[`PHASE_MAP.md`](PHASE_MAP.md) for the exact 69 -> 36 crosswalk.

Legend: `NOT STARTED` | `IN PROGRESS` | `PARTIAL` | `COMPLETE` | `BLOCKED`

| Phase | Spec | Title | Status | Notes |
|------:|:----:|-------|--------|-------|
| 0  | 0     | Project initialization | **COMPLETE** | Repo scaffold, venv (Python 3.12.10), Git, secrets excluded |
| 1  | 1-4   | Architecture, requirements, business questions & dataset design | **COMPLETE** | `docs/architecture.md`, `system-components.md`, `data-flow.md`, `security.md`, `requirements.md`, `business-questions.md`, `dataset-design.md` |
| 2  | 5-6   | Data generator & business data model | **COMPLETE** | `scripts/generate_dataset.py`; 150k clean rows, window 2026-01-01..2026-09-08, seed 20260909 -> `data/full_dataset.csv`; Revenue/Profit model asserted |
| 3  | 7-9   | Customer, product & geographical data | **COMPLETE** | `generate_dataset.py`: lognormal product `appeal` (top-decile revenue concentration), ~15% repeat customers via lognormal `purchase_weight` + boost, segment-driven basket size & discount, expanded Indian geography (23 states / 74 cities) with metro-weighted city demand + faster metro shipping; internal weight columns kept out of the 22-col CSV; determinism + all Phase 2 invariants preserved; `tests/test_phase03_customer_product_geo.py` |
| 4  | 10    | Seasonality engine | **COMPLETE** | `generate_dataset.py`: pure `day_seasonality_weights()` helper multiplies weekend uplift, last-3-day month-end uplift, curated `FESTIVE_PERIODS` sale windows (max, non-compounding) and a mid-Nov-peak annual cosine curve; mean-normalised, so only `Order_Date` distribution changes. `generate_orders` draws dates via `rng.choice(p=...)`; `summarise()` reports weekend/festive shares. Determinism + all Phase 2/3 invariants preserved; `tests/test_phase04_seasonality.py` |
| 5  | 11-12 | Data quality injection & business anomaly ground truth | **COMPLETE** | `generate_dataset.py`: `inject_data_quality_issues()` (9-category taxonomy -> `data/full_dataset_issues.csv` issue log, each defect mapped to a DQ dimension, ~1.2% of rows, `Revenue`/`Profit` left inconsistent) + `inject_business_anomaly()` (two-tier `West -> Electronics -> Laptop` anomaly over 2026-08-01..09-08: orders/revenue down, discount/returns/shipping up; rows re-derived so they stay valid). `build_delivery_dataset()` orchestrates clean -> anomaly -> issues; child RNGs `seed+1`/`seed+2` keep the clean stream + all Phase 2-4 tests untouched. `docs/anomaly-ground-truth.md` auto-generated with realised deltas. CLI `--clean` / `--no-anomaly` / `--no-issues`. Seed-reproducible (NFR-07). `tests/test_phase05_dataquality_anomaly.py` |
| 6  | 13    | Daily data generation & data/ lifecycle directories | **COMPLETE** | `generate_dataset.py`: `ensure_lifecycle_dirs()` creates `data/{incoming,raw,processed,rejected,archive}/` (`.gitkeep`-tracked, idempotent); `split_daily_files()` fans the delivery frame out into `data/incoming/sales_YYYY_MM_DD.csv` by calendar `Order_Date`, routing unparseable dates (a Phase 5 `invalid_date` defect) to `sales_invalid_date.csv` instead of dropping them; `generate_demo_day()` rebuilds the same seed's customer/product reference tables and draws one held-out day (`seed + 3`) for `2026-09-09`, written to `data/sales_2026_09_09.csv` (outside `data/incoming/` until the final killer demo). Wired into `main()` by default; `--no-daily-split` skips it, `--daily-dir` / `--demo-day-output` override the paths. `tests/test_phase06_daily_files.py` |
| 7  | 14    | PostgreSQL installation & configuration | **COMPLETE** | `config/docker-compose.postgres.yml` (PostgreSQL 16, all creds from `.env`, named volume `insightforge_pgdata`, `pg_isready` healthcheck; optional pgAdmin behind the `tools` profile); `config/postgres/initdb/01_bootstrap.sql` (first-boot: `insightforge` DB set to UTC + comment); `src/config.py` (`PostgresSettings.from_env()` assembles the SQLAlchemy URL from `POSTGRES_*` / honours `DATABASE_URL`; `safe_url` masks the password; placeholder-password guard fatal only in `production`); `scripts/postgres.py` (`up`/`down`/`status`/`logs`/`wait` around `docker compose`); `docs/database-setup.md` runbook. Star schema is Phase 8, `src/database.py` is Phase 9. `tests/test_phase07_postgres_config.py` (live-connection check opt-in via `INSIGHTFORGE_PG_INTEGRATION=1`) |
| 8  | 15-17 | Star schema, operational tables & indexing | **COMPLETE** | `sql/schema.sql` (idempotent, `IF NOT EXISTS` throughout): star schema `dim_date` / `dim_customer` / `dim_product` / `dim_region` / `fact_sales` (grain = one order line, surrogate `sales_key`, 4 dimension FKs + denormalised `order_date`/`customer_id`/`product_id`/`region`/`category`/`sub_category`/`customer_segment`, `is_returned` generated column, derived-measure CHECKs); operational tables `pipeline_runs` / `file_registry` / `data_quality_results` / `rejected_records` / `anomalies` / `recommendations` / `forecast_results` (all run-scoped rows FK `run_id -> pipeline_runs`); the six spec-named `fact_sales` indexes (`order_id`, `order_date`, `customer_id`, `product_id`, `region`, `category`) + FK / composite / operational indexes. `sql/drop_schema.sql` for dev reset; `scripts/apply_schema.py` (`--drop` / `--dry-run`, connects via `src.config`, reports table/index counts); `docs/database-schema.md` documents every table, relationship, lineage chain and index. `tests/test_phase08_schema.py` (static parse checks + opt-in `INSIGHTFORGE_PG_INTEGRATION=1` apply/introspection). |
| 9  | 18    | Database connection layer | **COMPLETE** | `src/database.py`: lazily-built pooled SQLAlchemy `Engine` (`get_engine()` / `dispose_engine()`, `pool_pre_ping`, env-tunable pool/timeouts; importing the module opens no connection) sourced from `src.config`. `Database` facade: `fetch_all` / `fetch_one` / `scalar`, `execute` / `execute_many` / `insert_returning` (each write in its own transaction), `transaction()` context manager (BEGIN/COMMIT/ROLLBACK), `ping()` / `healthcheck()`. Every statement via `sqlalchemy.text()` with bound `:name` params (SQL-injection safe). Retry policy (`tenacity`): transient failures (`OperationalError` / `InterfaceError` / invalidated connection) retried `DB_MAX_RETRIES` (3) with exponential backoff -> `DatabaseConnectionError`; non-transient -> `QueryExecutionError`; both sanitised (no SQL / params / DSN / host / stack trace), each carrying `.sqlstate` + `.cause_type`. `python -m src.database` self-check CLI. `docs/database-layer.md` full contract. `tests/test_phase09_database.py` (no-DB unit tests for the retry classifier / sanitiser / retry driver / engine singleton / facade with a stub engine + opt-in `INSIGHTFORGE_PG_INTEGRATION=1` live tests). |
| 10 | 19-20 | File ingestion & SHA-256 fingerprinting | **COMPLETE** | `src/ingestion.py`: `sha256_file()` (streamed 1 MiB blocks); `collect_metadata()` size/type/readable checks before parsing -> `FileMetadata`, else `IngestionError` (`.reason` never carries an absolute path); `ingest_file(path, db, paths)` - `file_registry` lookup by hash, mint `pipeline_runs` row, move `incoming->raw` + copy `raw->archive`, insert `file_registry` row; duplicate hash -> `SKIPPED_DUPLICATE` run + re-drop moved to `archive/` (`.dupe-<run_id>` on name collision), no `file_registry`/warehouse change; lost registration race (`SQLSTATE 23505`) degrades to `SKIPPED_DUPLICATE`; relocation/`FAILED` path sets a sanitised `error`. `watchdog` file watcher (`watch()` + `_IncomingHandler` with `.csv` filter and size-settle debounce, `WATCH_SETTLE_SECONDS`). `src/orchestrator.py`: single sequencer (`main(args)` for both entry points) - `--file` / `--scan` / `--watch` run for real, `--scheduler` returns 3 (Phase 35); an ingested run is closed `PARTIAL` with `stage_metrics.ingest` + a `data/processed/<name>.json` summary until validation/ETL land; exit codes 0/2/3/4(DB down, sanitised)/5. `src/config.py` gains `PipelinePaths` + `get_paths()` (resolves `DATA_*` / `LOGS_DIR` / `REPORTS_DIR`). `run_pipeline.py` gains `--watch`. `docs/ingestion.md` full contract. `tests/test_phase10_file_ingestion.py` (no-DB unit tests with a stub `Database` + real-`watchdog` smoke test + opt-in `INSIGHTFORGE_PG_INTEGRATION=1` ingest/dedupe/PARTIAL tests); `tests/test_phase00_scaffold.py` reworked (scheduler-not-ready). |
| 11 | 21-22 | Data contract & schema validation | **COMPLETE** | `config/data_contract.yaml`: declarative contract for the 22 fields - exact `columns` (order enforced), `allow_extra_columns: false`, 21 `mandatory` (all but `Shipping_Days`), per-field `type`/`pattern`/`min`/`max`/`values`/`nullable`/length, and `Category->Sub_Category` + `Region->State->City` hierarchies (transcribed from `scripts.generate_dataset` `CATALOG`/`GEOGRAPHY`); `uniqueness` declared but scored in Phase 14. `src/validation.py` (pure, no DB): `DataContract`/`FieldRule` + `@lru_cache get_contract()` (`DATA_CONTRACT_PATH` env), `validate_frame` / `validate_csv` -> `ValidationResult` (`structural_ok`, `structural_errors`, `row_violations` [`RowViolation`: `row_number`/`order_id`/`column`/`category`/`reason`], `rows_valid`/`rows_rejected`, `rejected_rows`). Categories: `missing_value`, `wrong_type`, `bad_pattern`, `out_of_range`, `not_in_allowed_values`, `invalid_hierarchy`, `bad_length`; `integer` accepts integral floats (`"4.0"`). Orchestrator stage 3: structural fail -> one `rejected_records` `'schema'` row + run `FAILED` + **exit code 6**; row violations -> `execute_many` one `rejected_records` row per bad data-row (raw row as JSONB) + run `PARTIAL` with `rows_valid`/`rows_rejected` + `stage_metrics.validate`. Scope = structural/contract only; the 7-dimension DQ score/gate stays Phase 14. `docs/validation.md`. `tests/test_phase11_schema_validation.py` (contract-vs-generator match, per-field/hierarchy/structural checks, orchestrator stage-3 with a stub `Database` + opt-in `INSIGHTFORGE_PG_INTEGRATION=1` live tests). |
| 12 | 23-24 | Alteryx ingestion & data-quality workflows | **COMPLETE** | `alteryx/01_ingestion.yxmd` + `alteryx/02_data_quality.yxmd` (hand-authored, parses as valid Alteryx XML; not executed against a real engine - none installed here); `src/config.py` gains `AlteryxSettings`/`get_alteryx_settings()`; `src/alteryx.py` (`WorkflowResult`, `run_ingestion_workflow`, `run_data_quality_workflow`, `invoke_alteryx_engine`) selects the real engine when configured (retried <=3, then Python fallback) or the Python fallback directly - the DQ fallback calls `src.validation.validate_csv` for parity with Phase 11. Orchestrator stage 4 merges `alteryx_ingestion`/`alteryx_dq` into `stage_metrics` and the processed-file summary; unexpected workflow failure closes the run `FAILED` with **exit code 7**. `docs/alteryx-workflows.md`. `tests/test_phase12_alteryx_workflows.py`. |
| 13 | 25-26 | Alteryx sales / customer / product ETL | **COMPLETE** | `alteryx/03_sales_etl.yxmd` + `alteryx/04_customer_product_etl.yxmd` (hand-authored, valid Alteryx XML; not executed against a real engine here); `src/etl.py` (`EtlResult`, `EtlLoadError`, `date_dim_row`, `recompute_revenue_profit`, `valid_rows_frame`, `load_valid_rows`, `run_sales_etl_workflow`) - the Python loader upserts `dim_customer`/`dim_product`/`dim_region`/`dim_date` (`ON CONFLICT ... DO NOTHING`, attributes fixed at first sight) and bulk-loads `fact_sales` for every contract-valid row inside one `db.transaction()`, re-deriving `Revenue`/`Profit` (FR-05) rather than trusting the CSV. Orchestrator stage 5 merges `stage_metrics.etl`, flips the run to **`SUCCESS`** (`error=NULL`), or - after 3 failed load attempts - `FAILED` with new **exit code 8**. `docs/star-schema-etl.md`. `tests/test_phase13_star_schema_etl.py` (live-load assertions opt-in via `INSIGHTFORGE_PG_INTEGRATION=1`; no Postgres running in this environment, so only the unit/control-flow tests have actually executed). |
| 14 | 27-29 | Data quality engine, score & rejected-record management | **COMPLETE** | `src/data_quality.py` (`DIMENSIONS`, `DimensionResult`, `DqReport`, `thresholds()`, `score_file()`) scores 7 dimensions - Completeness/Validity/Referential Integrity reuse Phase 11's `ValidationResult.row_violations`; Uniqueness (full-row duplicates per the contract's declared `uniqueness: {row: all_columns}`), Consistency (Revenue/Profit re-derivation via `src.etl.recompute_revenue_profit`), Accuracy (`Order_Status`<->`Shipping_Days` rule) and Timeliness (`Order_Date` vs the `sales_YYYY_MM_DD.csv` batch date, skipped for non-standard filenames) are new. Overall score is the unweighted mean (spec gives no per-dimension formulas or weights - documented as this project's own operational definition in `docs/data-quality-engine.md`). Gate: `>=95` PASS -> `SUCCESS`, `90-94.99` WARNING -> new `WARNING` status (`sql/schema.sql`'s `pipeline_runs.status` CHECK widened, idempotently, via an `ALTER TABLE`), `<90` REJECT -> `FAILED` + **exit code 9** - REJECT is audit-only and never rolls back `fact_sales` rows Phase 13 already loaded (`fact_sales` is insert-only per run). Orchestrator stage 6 persists 8 `data_quality_results` rows per run (7 dimensions + `Overall`) and sets `pipeline_runs.dq_score`/`stage_metrics.dq`. `tests/test_phase14_data_quality_engine.py`. |
| 15 | 30-31 | Core SQL KPI engine & advanced SQL analytics | **COMPLETE** | `sql/kpi_queries.sql` - 10 standalone `-- @query:`-marked queries computing Revenue/Profit/Margin/Orders/Customers/Units/AOV/Return Rate/Average Discount/Shipping Time across all 6 FR-08 grains (whole-dataset, day, month, region, category, product, customer), demonstrating every named technique (JOIN, CASE, CTE, subqueries, window functions, RANK/DENSE_RANK, LAG/LEAD, rolling average, running total). Formulas are this project's own operational definition - the spec names metrics/techniques but not formulas (documented in `docs/kpi-engine.md`). Pure SQL, no Python wrapper (matches the spec: the first Python analytics module is `src/change_detection.py`, Phase 17). `tests/test_phase15_kpi_queries.py` (static keyword/marker checks always run; live correctness checks against independently-computed pandas aggregates are `INSIGHTFORGE_PG_INTEGRATION=1`-gated - no Postgres running in this environment, so only the static tests have actually executed). |
| 16 | 32    | Analytical views | **COMPLETE** | `sql/views.sql` - `CREATE OR REPLACE VIEW` for all 6 spec-named views (`daily_kpis`, `monthly_kpis`, `regional_performance`, `category_performance`, `product_performance`, `customer_performance`), reusing Phase 15's formulas, complete (unfiltered) at every grain. `scripts/apply_schema.py` now applies `views.sql` right after `schema.sql` and reports `views present: X/6`. `docs/analytical-views.md`. `tests/test_phase16_analytical_views.py` (static checks always run; live checks are `INSIGHTFORGE_PG_INTEGRATION=1`-gated - no Postgres running in this environment, so only the static tests have actually executed). |
| 17 | 33-34 | Period comparison & business change detection | **COMPLETE** | `src/change_detection.py` - first Python analytics module. `compare_period(db, grain)`/`compare_all_periods(db)` compare the two most recent complete periods (day/week/month vs previous - spec-named grains only) across the 10 KPIs, querying `fact_sales` directly per grain (not the Phase 16 views - avoids double-counting distinct-count metrics like `customers` at the week grain). Pure core (`build_change_records`, `_pct_change`, `_direction`) needs no DB. `significant_change_threshold_pct()` reads new `CHANGE_DETECTION_THRESHOLD_PCT` (default 10.0) - spec gives an example output shape but no threshold. Not persisted (no schema table for it) and not wired into `src/orchestrator.py` - a downstream/batch analytics module. `docs/change-detection.md`. `tests/test_phase17_change_detection.py` (pure-logic tests always run; live-Postgres cross-checks against pandas are `INSIGHTFORGE_PG_INTEGRATION=1`-gated - no Postgres running in this environment, so only the pure tests have actually executed). |
| 18 | 35-36 | Z-score & IQR anomaly detection | **COMPLETE** | `src/anomaly_detection.py` - `detect_zscore`/`detect_iqr` (pure, no DB) plus `detect_all_zscore`/`detect_all_iqr`/`daily_metric_series` (query `fact_sales` via `src.change_detection.fetch_period_series`, reused rather than a 4th copy of the 10-metric SQL). New `ZSCORE_THRESHOLD` (3.0) / `IQR_MULTIPLIER` (1.5) env defaults - spec names the two methods but no numeric threshold. Not persisted (fusion + the `anomalies` table write is Phase 20) and not orchestrator-wired, same shape as Phase 17. `docs/anomaly-detection.md`. `tests/test_phase18_anomaly_detection.py` (pure-logic tests always run; live-Postgres injected-outlier test is `INSIGHTFORGE_PG_INTEGRATION=1`-gated - no Postgres running in this environment, so only the pure tests have actually executed). |
| 19 | 37-38 | Rolling baseline & Isolation Forest | **COMPLETE** | Extends `src/anomaly_detection.py` (same module as Phase 18): `detect_rolling_baseline` (trailing `ROLLING_WINDOW_DAYS`=7 window, mean/median/std, `ROLLING_BASELINE_MULTIPLIER`=2.0 band - deliberately trailing/causal, unlike Z-score/IQR's whole-series baseline) and `detect_isolation_forest` (`sklearn.ensemble.IsolationForest` on the metric's own daily values, `IFOREST_CONTAMINATION`="auto", fixed `IFOREST_RANDOM_STATE`=42 for determinism; anomaly score = negated `decision_function`, confidence = a logistic squash of it - this project's own mapping). Plus `detect_all_rolling_baseline`/`detect_all_isolation_forest` DB wrappers. Still not persisted (Phase 20 fuses all four detectors and writes `anomalies`). `docs/anomaly-detection.md` (rewritten to cover all four). `tests/test_phase19_rolling_isolation_forest.py` (pure-logic tests always run, including a determinism check; live-Postgres injected-outlier test is `INSIGHTFORGE_PG_INTEGRATION=1`-gated - no Postgres running in this environment). |
| 20 | 39-40 | Anomaly fusion & severity engine | **COMPLETE** | `src/anomaly_fusion.py` - `fuse_metric_series`/`fuse_all_metrics` combine the four Phase 18-19 detectors per metric/date, gated by `ANOMALY_FUSION_MIN_VOTES`=2 (only dates with >=2 of 4 detectors agreeing get a row); `expected_value` prefers the rolling baseline's mean, `confidence`=votes/4, `persistence_days`=consecutive-streak count; severity score literally uses all 4 FR-12 factors (deviation, `BUSINESS_IMPACT_WEIGHT` per metric, confidence, persistence) classified LOW/MEDIUM/HIGH/CRITICAL. `persist_anomalies`/`detect_and_persist_anomalies` write to `anomalies` - the only anomaly-related module that touches the DB, since `anomalies.run_id` is `NOT NULL`. **Wired into `src/orchestrator.py`** as stage 7 (unlike Phases 17-19's unwired pure detectors) - runs for the just-ingested file's own dates only, purely additive to `stage_metrics.anomalies`, never affects `pipeline_runs.status` (a failure here is caught and logged, never fails the run). `docs/anomaly-fusion.md`. `tests/test_phase20_anomaly_fusion.py` (fusion-math tests against a stub DB always run; live-Postgres + orchestrator-wiring test is `INSIGHTFORGE_PG_INTEGRATION=1`-gated - no Postgres running in this environment). |
| 21 | 41-42 | Data drift detection & drift reporting | **COMPLETE** | `src/drift_detection.py` - monitors FR-13's 6 distributions (price/quantity/discount/shipping continuous, category/region mix categorical) via PSI (Population Stability Index - an external, industry-standard method/thresholds, not invented here: `<0.1` Normal, `0.1-0.25` Warning, `>=0.25` Drift Detected). Compares a trailing `DRIFT_CURRENT_DAYS`=7 window against the `DRIFT_BASELINE_DAYS`=30 days before it; continuous features binned by the baseline's own deciles, categorical features use natural categories. New `drift_results` table (`sql/schema.sql`, applied by `scripts/apply_schema.py`) - the only drift artifact persisted, since `sql/schema.sql` had no drift table before this phase. **Wired into `src/orchestrator.py`** as stage 8 (same reasoning as Phase 20 - `run_id NOT NULL`), purely additive to `stage_metrics.drift`, never affects `pipeline_runs.status`. `docs/drift-detection.md`. `tests/test_phase21_drift_detection.py` (PSI-math tests always run; live-Postgres + orchestrator-wiring test is `INSIGHTFORGE_PG_INTEGRATION=1`-gated - no Postgres running in this environment). |
| 22 | 43-45 | Root-cause engine, contribution analysis & RCA confidence | **COMPLETE** | `src/root_cause.py` - pure, on-demand module (no DB writes, not orchestrator-wired - confirmed via no RCA table in `sql/schema.sql`, `data-flow.md` stage 11's plain-text "Writes" column, and `business-questions.md` describing RCA as invoked, not automatic). `contribution_by_dimension`/`drill_down` implement the spec's nested Region->Category->Subcategory->Product->Customer Segment drill-down; contribution = current-minus-previous sum per dimension value (reproduces the spec's own West/South/North/Other worked example exactly); scoped to `RCA_SUPPORTED_METRICS`=(revenue, profit, units) since ratio KPIs can't be additively decomposed without fabricating a number. `analyze_root_cause` assembles the spec's 6 required fields (Metric/Change/Primary Driver/Evidence/Contribution/Confidence) plus the full tier breakdown; confidence = `0.7*concentration + 0.3*evidence_strength` (`RCA_MIN_EVIDENCE_ROWS`=30). `docs/root-cause-analysis.md`. `tests/test_phase22_root_cause.py` (pure drill-down/contribution tests always run; live-Postgres skewed-region test is `INSIGHTFORGE_PG_INTEGRATION=1`-gated - no Postgres running in this environment). |
| 23 | 46    | Business impact engine | **COMPLETE** | `src/impact_analysis.py` - pure, on-demand module (no DB writes, not orchestrator-wired, same shape as Phase 17/22). "Expected" reuses Phase 19's rolling baseline mean (its own documented "expected range") for revenue/profit rather than inventing a new baseline - no new env vars, reuses `ROLLING_WINDOW_DAYS`/`ROLLING_BASELINE_MULTIPLIER`. `compute_gap` (pure): `gap = actual - expected` (signed), `at_risk = max(0.0, expected - actual)` (only a shortfall counts as risk). `assess_business_impact(db, date=None, ...)` assembles the spec's 10 fields (Expected/Actual/Gap for Revenue & Profit, Revenue/Profit at Risk, Customers/Orders Affected - the latter two via a `fact_sales` count-distinct query for the assessed date, same shape as `root_cause.py`'s fact-row fetch); `assess_business_impact_series` covers every date the rolling baseline reaches. `docs/business-impact-engine.md`. `tests/test_phase23_business_impact.py` (pure `compute_gap` + stub-DB assembly tests always run; live-Postgres integration test is `INSIGHTFORGE_PG_INTEGRATION=1`-gated - no Postgres running in this environment, so only the unit tests have actually executed). |
| 24 | 47-48 | Customer RFM & product intelligence | **COMPLETE** | `src/rfm.py` - pure, on-demand module (no DB writes, not orchestrator-wired). Recency measured against `MAX(order_date)` in `fact_sales` (dataset-relative, never wall-clock); Recency/Frequency/Monetary quintile-scored 1-5 via `numpy.percentile` (`score_quintiles`); `classify_segment(r, f, m)` is a priority-ordered rule covering every combination, mapping to Champions/Loyal/Potential Loyalists/New/At Risk/Lost (verified exhaustively over all 125 `(r,f,m)` combos). `src/product_intelligence.py` - reuses Phase 16's `product_performance` view for base aggregates; adds a trailing-vs-prior `PRODUCT_GROWTH_WINDOW_DAYS`-day (30) revenue comparison for growth (`None` when the prior window has no revenue, never fabricated); quartile-relative flags for High Revenue/Profit/Return, Low Margin, Slow Moving; Fast Growing/Declining via `PRODUCT_GROWTH_THRESHOLD_PCT` (20.0); Star = high revenue + high profit + fast growing; health score (0-100) = weighted composite of profit/margin/growth/return percentile ranks (0.35/0.25/0.20/0.20), same shape as Phase 20's severity engine. `docs/customer-rfm-analysis.md`, `docs/product-intelligence.md`. `tests/test_phase24_rfm_product_intelligence.py` (pure + stub-DB tests always run; live-Postgres integration tests are `INSIGHTFORGE_PG_INTEGRATION=1`-gated - no Postgres running in this environment, so only the unit tests have actually executed). |
| 25 | 49-50 | Forecasting & forecast evaluation | NOT STARTED | |
| 26 | 51-52 | Recommendation engine & priority engine | NOT STARTED | |
| 27 | 53    | AI evidence layer | NOT STARTED | Gemini (decided) |
| 28 | 54-55 | AI Analyst & AI explanation engine | NOT STARTED | |
| 29 | 56-57 | Natural-language-to-SQL & AI security | NOT STARTED | |
| 30 | 58-59 | Streamlit application & pipeline control | NOT STARTED | |
| 31 | 60    | Power BI data model | NOT STARTED | |
| 32 | 61-63 | Power BI executive, intelligence & forecast/pipeline pages | NOT STARTED | |
| 33 | 64-65 | Automated Excel & PDF reports | NOT STARTED | |
| 34 | 66    | Alert & email engine | NOT STARTED | |
| 35 | 67    | Observability & failure recovery | NOT STARTED | includes scheduler wiring |
| 36 | 68-69 | Security, testing, performance & final end-to-end delivery | NOT STARTED | |

## Environment notes

- **Python**: 3.12.10, project venv at `.venv/` (installed via winget).
- **PostgreSQL**: provisioned via Docker in Phase 7 (`config/docker-compose.postgres.yml`;
  helper `scripts/postgres.py`, config `src/config.py`, runbook `docs/database-setup.md`).
- **Alteryx**: not installed in this environment. Real `.yxmd` workflow files plus a
  Python ETL fallback (identical contract output) will be built; automated Alteryx
  execution will be documented as unverified rather than faked.
- **Power BI Desktop**: installed. Automated dataset refresh is a documented manual/gateway step.
- **LLM**: Google Gemini via `google-generativeai`; deterministic template fallback when no key.
- **Docker**: available (v29.7.2).
